# Copy to .env and fill in your own Groq key.
# Without a key, the system runs in offline-mock mode (hash-based fake AI).
# Get a real key at https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
