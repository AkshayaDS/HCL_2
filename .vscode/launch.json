{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "HCL AI Force — run.py",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "justMyCode": true,
      "env": { "FLASK_ENV": "development" }
    }
  ]
}
